import { CanvasColorPickerDirective } from './canvas-color-picker.directive';

describe('CanvasColorPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasColorPickerDirective();
    expect(directive).toBeTruthy();
  });
});
