import { ShortcutDirective } from './shortcut.directive';

describe('ShortcutDirective', () => {
  it('should create an instance', () => {
    const directive = new ShortcutDirective();
    expect(directive).toBeTruthy();
  });
});
